<Window x:Class="FactorySADEfficiencyOptimizer.UIComponent.IndividualTargetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d3="clr-namespace:InteractiveDataDisplay.WPF;assembly=InteractiveDataDisplay.WPF"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:FactorySADEfficiencyOptimizer.UIComponent"
        mc:Ignorable="d" DataContext="{Binding RelativeSource={RelativeSource Mode=Self}, Path=ItemModel, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
        Title="IndividualTargetWindow" Height="450" Width="800">
    <Window.Resources>
        <!-- 
            Window.Resources utility learned from this StackOverflow resource:
            https://stackoverflow.com/questions/932510/how-do-i-space-out-the-child-elements-of-a-stackpanel
        -->
        <Thickness x:Key="ResultsBannerItemMargin">0,0,0,10</Thickness>
    </Window.Resources>
    <Grid Background="DarkGray">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"></ColumnDefinition>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="2"></ColumnDefinition>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="31*"></RowDefinition>
            <RowDefinition Height="56*"/>
            <RowDefinition Height="347*"></RowDefinition>
        </Grid.RowDefinitions>
        <Border Grid.RowSpan="2" Grid.ColumnSpan="2" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="20 20 0 0">
            <Border Width="{Binding ElementName=ResultsBlock, Path=ActualWidth, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0 0 0 1" BorderBrush="Black">
                <TextBlock x:Name="ResultsBlock" Text="Results:" FontSize="36"></TextBlock>
            </Border>
        </Border>


        <Border Grid.Column="0" Grid.Row="2" Margin="20,0,10,20" Grid.ColumnSpan="2">
            <StackPanel DataContext="{Binding ProductionTarget, Mode=OneTime}" >
                <StackPanel.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="Margin" Value="{StaticResource ResultsBannerItemMargin}" />
                    </Style>
                </StackPanel.Resources>
                <Border BorderBrush="Black" BorderThickness="1" Height="34">
                    <TextBlock Grid.ColumnSpan="3" Text="{Binding ProductTarget.ProductName, Mode=OneTime, StringFormat=Item: {0}}" FontSize="24"></TextBlock>
                </Border>
                <Border BorderBrush="Black" BorderThickness="1" Height="34">
                    <DockPanel>
                        <TextBlock Text="Target: " HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="24"></TextBlock>
                        <TextBlock Text="{Binding TargetQuantity, Mode=OneTime}" FontSize="24" HorizontalAlignment="Left" VerticalAlignment="Top"></TextBlock>
                        <TextBlock Text="Quantity: " HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="24"></TextBlock>
                        <TextBlock Text="{Binding CurrentAmount, Mode=OneTime}" HorizontalAlignment="Right" VerticalAlignment="Top" FontSize="24"></TextBlock>
                    </DockPanel>
                </Border>
                <Border BorderBrush="Black" BorderThickness="1">
                    <DockPanel>
                        <TextBlock Text="Target Date: " FontSize="24"></TextBlock>
                        <TextBlock Text="{Binding DueDate, Mode=OneTime}" FontSize="24" Margin="0 0 30 0"></TextBlock>
                        <TextBlock Text="Completed: " FontSize="24"></TextBlock>
                        <TextBlock Text=""></TextBlock>
                    </DockPanel>
                </Border>
            </StackPanel>
        </Border>

        <Border x:Name="IndividualTargetGraph">
            
        </Border>
    </Grid>
</Window>
